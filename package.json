{
  "name": "network-claude-peers-mcp",
  "version": "0.1.0",
  "description": "Networked MCP server that lets Claude Code sessions discover and message each other across directories, machines, and Docker containers",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "broker": "bun broker.ts",
    "server": "bun server.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  }
}
